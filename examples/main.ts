import { createApp } from 'vue'
// Import the stylesheet exactly as a consumer would.
import 'awesome-graphs-vue3/style.css'
import App from './App.vue'
import './styles.css'

createApp(App).mount('#app')
